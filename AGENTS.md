# AGENTS.md — สรุปงาน JobCert

## เว็บนี้คืออะไร

JobCert คือเว็บสมัครงานสาย AI/ดิจิทัล มีอบรม/สัมมนา กับสอบ Certify รวมอยู่ด้วย
เอาเทมเพลต Bootstrap ฟรีชื่อ LifeSure มาดัดแปลง (ของเดิมเป็นเว็บขายประกันชีวิต) เปลี่ยนสี
เปลี่ยนเนื้อหา ใส่ฟอร์ม ใส่สคริปต์เอง

**เว็บรวมเป็นไฟล์เดียวแล้ว** — ก่อนหน้าแยกเป็น 6 หน้า (index, job, training, certify, about, contact)
ตอนนี้รวมเป็น `index.html` ไฟล์เดียว ทำเป็น SPA เอา แต่ละหน้าเดิมกลายเป็น
`<section id="page-xxx">` อยู่ในไฟล์เดียวกันหมด แล้วสลับโชว์/ซ่อนด้วย JS (ฟังก์ชัน `แสดงหน้า('ชื่อหน้า')`
ใน main.js) กดเมนูแล้วไม่มีการโหลดหน้าใหม่ แค่ซ่อน section เก่า โชว์ section ใหม่ อัปเดต URL hash
ไปด้วย (เช่น `index.html#job`) เผื่อใครแชร์ลิงก์ตรงมาที่หน้านั้นได้เลย

**ข้อควรระวัง**: section ที่ไม่ใช่หน้าแรกจะเริ่มต้นด้วย `display:none` ห้ามใส่ class `wow`
(แอนิเมชันตอนสกอลล์) ลงไปเด็ดขาด เพราะ wow.js มันจะไปเซ็ต `visibility:hidden` ค้างไว้ถ้า element
โดน parent ซ่อนอยู่ตอนโหลดหน้า แล้วต่อให้ JS สั่ง `display:block` ทีหลัง มันก็ไม่โผล่ขึ้นมาอยู่ดี
(เจอบั๊กนี้มาแล้วตอนทำการ์ดสมัครงานด้วย ถ้าจะเพิ่มอะไรใหม่ในหน้าที่ไม่ใช่หน้าแรก อย่าลืมเช็คจุดนี้)

**js รวมเป็นไฟล์เดียวด้วย** — เมื่อก่อนโค้ดเช็คฟอร์ม/กรองงาน/สลับหน้า ฝังเป็น `<script>` แยกท่อน
อยู่ท้าย index.html หลายก้อน ตอนนี้ย้ายมารวมไว้ใน `js/main.js` หมดแล้ว ในไฟล์เดียว เวลาจะแก้อะไรที่
เกี่ยวกับฟอร์มหรือการสลับหน้า ไปดูที่ main.js ได้เลย ไม่ต้องเปิด index.html ไล่หา `<script>` อีกต่อไป
(เก็บสไตล์เดิมไว้ทุกอย่างนะ ฟังก์ชันชื่อไทยปนอังกฤษ ใช้ `function`/`var` ธรรมดา ไม่มีอะไรเปลี่ยน
แค่ย้ายที่อยู่)

**รูป** — โฟลเดอร์ `img/` :
- รูปทีมผู้พัฒนา 2 คนในหน้าเกี่ยวกับเรา — ไฟล์จริงคือ `member1.png` = ชินวัตร กับ `member2.jpg` = ศรีรัตน์
  (นามสกุลไฟล์เคยจดผิดในเอกสารรุ่นก่อนๆ ว่า member1 เป็น .jpg และ member2 เป็น .png สลับกัน เช็คจาก
  `alt=` ใน index.html แล้วอันนี้ถูกต้อง) วงกลมขยายใหญ่ขึ้นเป็น 190px แล้ว (เดิม 110px เล็กไป) ใช้
  `.member-photo` + `.member-photo img` คู่กับ `object-fit: cover` ครอปกลางภาพอัตโนมัติเฉยๆ
  **เช็คใน css/style.css แล้วไม่มี** `transform: scale()` หรือ `object-position` เจาะจงรูปไหนเป็นพิเศษ
  (เอกสารรุ่นก่อนเขียนว่ามีการ zoom เข้าไปเพราะรูปหนึ่งถ่ายมาตัวเล็ก แต่โค้ดจริงไม่มี — ถ้าเปิดเว็บแล้ว
  เห็นรูปใครหน้าไม่อยู่กลาง/ครอปแปลกๆ ต้องมาเพิ่ม object-position เองใหม่ ยังไม่ได้ทำ)
- การ์ดสอบ Certify ครบ 6 ใบแล้ว (ไฟล์ชื่อ `cert-xxx.png` ตรงกับชื่อวิชาเป๊ะๆ) กล่องรูป
  `.cert-image-placeholder` ขยายจาก 140px เป็น 200px แล้วด้วย เพราะตอนแรกใส่รูปเข้าไปแล้วมันดูบีบ
  เตี้ยไป กับเอาเส้นประ (ของตอนที่ยังไม่มีรูป) ออกด้วย เพราะตอนนี้มีรูปจริงแล้วใส่กรอบทึบแทน
- ภาพประกอบใหญ่ๆ ที่เคยเป็นไอคอน Font Awesome จางๆ (หุ่นยนต์ในแบนเนอร์แรก, ตรารับรองในแบนเนอร์สอง,
  เครื่องหมายคำถามในโซน FAQ) เปลี่ยนเป็นรูปภาพประกอบจริงแล้วทั้ง 3 จุด
- **อัปเดตล่าสุด**: เช็คด้วย grep ทั้ง index.html และ css/style.css แล้วพบว่า `1.png`, `2.png`, `3.png`,
  `4.png` ที่ค้างอยู่ใน img/ ไม่ได้ถูกอ้างถึงที่ไหนเลยสักที่ (เอกสารรุ่นก่อนจำผิดว่าไฟล์ที่เหลือชื่อ `5.png`/`6.png`
  แต่จริงๆ ไม่เคยมีไฟล์ชื่อนั้นในโฟลเดอร์เลย) ลบทิ้งไปแล้วเพราะเป็นขยะเปล่าๆ ไม่ได้ใช้ที่ไหน

**เอาบางส่วนออกจากหน้าแรกแล้ว** — การ์ด "ทีมงานพร้อมช่วยเหลือ" ในโซนจุดเด่น (Feature) กับทั้งบล็อก
"บริการของเรา / 3 บริการหลักของ JobCert" (Service section) เอาออกทั้งคู่แล้วตามที่สั่ง โซน Feature
ตอนนี้เหลือ 3 การ์ด (งาน/อบรม/certify) ปรับ column ให้เต็มแถวพอดีแล้ว

**⚠️ จุดที่น่าจะลืมไป**: พอเอา Service section กับการ์ดนั้นออก ปรากฏว่าตอนนี้ **ไม่มีลิงก์ไหนในเว็บ
พาไปหน้าติดต่อเรา (page-contact) เลย** เมนู nav ก็ไม่มี footer ก็ไม่มี หน้านั้นเลยเข้าถึงไม่ได้แล้ว
นอกจากพิมพ์ `index.html#contact` เอาเอง น่าจะต้องหาที่ใส่ลิงก์กลับเข้าไปใหม่ (เช่น เพิ่มใน nav
หรือ footer) ไม่งั้นฟอร์มติดต่อที่ทำไว้จะไม่มีใครเจอ

**เมนู nav ตอนนี้**: หน้าแรก, สมัครงาน AI/ดิจิทัล, อบรม/สัมมนา, สอบ Certify, เกี่ยวกับเรา
(เพิ่ม "สมัครงาน AI/ดิจิทัล" กลับเข้ามาแล้วตามที่ขอ — ของเดิมเคยเอาออกเพราะซ้ำกับปุ่ม CTA
"สมัครงานเลย" แต่ตอนนี้ใส่กลับมาเป็น nav-link ปกติ ส่วนปุ่ม CTA สีน้ำเงินก็ยังอยู่เหมือนเดิม)

**ชื่อทีมผู้พัฒนาใส่ครบแล้ว** — ชินวัตร อ่วมแก้ว (1660701283) กับ ศรีรัตน์ อินทลัย (1660705185)
การ์ดตอนนี้โชว์แค่ชื่อ+รหัสนักศึกษา ไม่มีอีเมล/เบอร์โทรต่อคนแล้ว (ตัดออกไปตอนไหนไม่รู้ อาจจะเจ้าของ
โปรเจกต์แก้เองในเครื่อง ถ้าอยากได้อีเมล/เบอร์ต่อคนคืนมาต้องไปดูโครงสร้างการ์ดเพิ่ม)

## เช็คโค้ดทั้งไฟล์แล้ว (ผลตรวจล่าสุด)

ไล่เช็คทุกไฟล์แบบ cross-reference ระหว่าง index.html กับ js/main.js รอบนึง สรุปผล:

**เช็คแล้วโอเค ไม่มีบั๊ก:**
- ฟังก์ชันที่ index.html เรียกผ่าน onclick/onsubmit/oninput ทั้ง 28 ตัว ตรงกับที่ประกาศไว้ใน main.js
  ครบทุกตัว ไม่มีเรียกฟังก์ชันที่ไม่มีอยู่จริง และไม่มีฟังก์ชันไหนประกาศทิ้งไว้เฉยๆ ไม่มีใครเรียก
- id ทั้งหมดที่ main.js เรียกผ่าน `getElementById` มีอยู่จริงในหน้า HTML ครบ ไม่มี id พิมพ์ผิด
- ไม่มี id ซ้ำกันเลยทั้งไฟล์ (เช็คจาก 143 id ทั้งหน้า)
- id ที่ส่งเข้าแบบ dynamic ผ่าน `แสดงฟอร์ม('form_xxx')` / `แสดงคอร์ส(...)` / `แสดงวิชาสอบ(...)` ตรงกับ
  id ของฟอร์มจริงทุกอัน
- ตรวจ class `wow` ทั้งไฟล์แล้ว ไม่มีหลุดไปอยู่ใน section ที่ซ่อนอยู่ (page-job/training/certify/about/
  contact) เลย มีแต่ใน page-home (ซึ่งโชว์อยู่แล้วตั้งแต่โหลดหน้า ไม่ติดบั๊กที่เอกสารเตือนไว้) กับที่ footer
  ท้ายสุดซึ่งอยู่นอก section ทั้งหมด ไม่โดนซ่อนตอนโหลดเหมือนกัน เลยไม่มีปัญหา
- `data-bs-target` / `aria-controls` ของ accordion (คอร์ส/สอบ certify/FAQ) ชี้ไปหา id ที่มีอยู่จริงครบ
- `<label for="...">` ทั้ง 83 ตัว จับคู่กับ input id ที่มีจริงครบ
- css/style.css เช็ค `{` `}` บาลานซ์กันครบ ไม่มี syntax error
- ช่องค้นหางาน (jobSearchInput/jobLocationInput) ไม่ได้ผูก event ไว้ที่ตัว input เอง แต่มีปุ่ม "ค้นหางาน"
  แยกต่างหากที่เรียก `กรองงาน()` ให้ — ตั้งใจทำแบบกดปุ่มค้นหา ไม่ใช่ live search เลยไม่ใช่บั๊ก

**ที่ยังเป็นปัญหาอยู่ (ของเดิม ยังไม่ได้แก้):**
- หน้าติดต่อเรา (`page-contact`) เข้าไม่ถึงจากที่ไหนเลยจริงๆ (ดูหัวข้อ "จุดที่น่าจะลืมไป" ด้านบน กับข้อ 1
  ในลิสต์ "ยังทำไม่เสร็จ" ด้านล่าง) — เช็คซ้ำแล้วยืนยันว่าทั้ง nav และ footer ไม่มีลิงก์ไปหน้านี้เลยจริงๆ

## อันนี้ก็ยังทำไม่เสร็จ ต้องมาต่อ

1. **หน้าติดต่อเราหาไม่เจอแล้ว** (ตามที่บอกข้างบน) ต้องคุยกับเจ้าของโปรเจกต์ว่าจะเอาลิงก์ไปวางตรงไหนดี
2. **รหัสนักศึกษาคนที่ 3 (ถ้ามี) + ชื่อไฟล์ zip ตอนส่งงาน** — สเปกให้ตั้งชื่อ
   `mini_proj1_[ID1]_[ID2]_[ID3].zip` ตอนนี้มีรหัส 2 คนแล้ว (ดูด้านบน) เช็คกับเจ้าของโปรเจกต์อีกทีว่า
   มีคนที่ 3 ไหม แล้วค่อยแพ็กไฟล์ส่ง
3. **โฟลเดอร์ `img/` ยังไม่เปลี่ยนชื่อเป็น `images/`** ตามที่สเปกอยากได้ — ยังไม่ได้ทำ
4. **หน้ารวมอบรม+Certify (`courses.html` ตามสเปก)** — ตอนนี้ยัง section แยกกัน (`page-training`,
   `page-certify`) อยู่ในไฟล์เดียวกันก็จริง แต่ยังคนละหน้า/คนละดีไซน์กันอยู่ ยังไม่ได้ทำให้เป็นหน้าเดียว
   ตามสเปกจริงๆ
5. **ฟอร์ม Talent Registration & AI Skill Profiling** — สเปกอยากได้ฟอร์มรวมอีกแบบ มีคำนำหน้า,
   ระดับการศึกษา, AI Skill Tier (Beginner/Pioneer/Innovator), เลือกตำแหน่งงานที่สนใจ, และ checkbox
   ยินยอม PDPA แบบบังคับติ๊ก ยังไม่ได้ทำเลย ต้องคิดว่าจะวางไว้หน้าไหนดี ไม่ให้ซ้อนกับฟอร์มสมัครงาน
   ต่อใบที่มีอยู่แล้วจนคนงง

## โครงสร้างไฟล์ตอนนี้

```
index.html   ← เว็บทั้งเว็บอยู่นี่ที่เดียว แบ่งเป็น section: page-home, page-job, page-training,
               page-certify, page-about, page-contact สลับกันด้วย JS ในไฟล์นี้ไม่มี <script> ฝังแล้ว
               (ย้ายไป main.js หมด) มีแค่ <script src="js/main.js"> โผล่ตอนท้าย
css/style.css        ← ธีมสี + style ที่ปรับเอง (มี comment "ปรับสีธีมเว็บใหม่" อยู่บนสุด)
css/bootstrap.min.css← ของเทมเพลตเดิม ไม่ต้องไปแก้ ไปแก้ที่ style.css แทน
js/main.js           ← js ทุกอย่างอยู่นี่ที่เดียวแล้ว ครึ่งแรกเป็นสคริปต์เทมเพลตเดิม (wow.js, owl
                        carousel, sticky nav ฯลฯ) ครึ่งหลัง (มีคอมเมนต์คั่นไว้ชัดเจน) เป็นโค้ดของเราเอง
                        ทั้งหมด (สลับหน้า, กรองงาน, เช็คฟอร์มทุกฟอร์ม) — ก่อนหน้านี้พวกนี้เคยฝังเป็น
                        <script> แยกท่อนอยู่ใน index.html ตอนนี้ย้ายมารวมที่นี่แล้ว
img/                 ← มีรูปจริงใส่แล้วเกือบหมด (ดูหัวข้อด้านบน) รูปที่ไม่ได้ใช้ (1-4.png เดิม) ลบทิ้ง
                        ไปแล้ว ในโฟลเดอร์ตอนนี้เหลือแต่ไฟล์ที่ index.html เรียกใช้จริงทั้งหมด
                        ยังไม่ได้เปลี่ยนชื่อโฟลเดอร์เป็น images/ ตามสเปก
lib/                  ← ไลบรารีหน้าบ้าน เหลือแค่ที่ใช้จริง (animate, counterup, easing, owlcarousel,
                        waypoints, wow) ตัดของที่ไม่ได้ใช้ออกหมดแล้ว
```

## สีธีม (ฟ้า–น้ำเงิน–ขาว)

อยู่บนสุดของ `css/style.css` หา comment "ปรับสีธีมเว็บใหม่" เจอแน่นอน:

| สี | โค้ดสี | ใช้ตรงไหน |
|---|---|---|
| ขาว/เทาอ่อน | `#F6F6F6` | `--bs-light`, พื้นหลังโซนอ่อน |
| ฟ้าอ่อน | `#D6E4F0` | `--bs-secondary`, สีรอง |
| ฟ้าน้ำเงิน (หลัก) | `#1E56A0` | `--bs-primary`, ปุ่ม/ลิงก์/หัวข้อเน้น |
| น้ำเงินเข้ม | `#163172` | `--bs-dark`, footer/หัวข้อใหญ่ |

ข้อควรระวัง: bootstrap.min.css รุ่นนี้ฝังสี hex ตรงๆ ไว้ในคลาส `.bg-primary` `.text-primary`
`.btn-primary` ไม่ได้ใช้ `var(--bs-primary)` อัตโนมัติ เพราะงั้นแก้แค่ `:root` เฉยๆ ไม่พอ ต้องมี rule
เขียนทับคลาสพวกนี้อีกทีใน style.css (ทำไว้ให้แล้ว) จะเปลี่ยนสีอีกรอบก็แก้บล็อกนี้บล็อกเดียวพอ

## สไตล์โค้ด (ตรงนี้อย่าไปแตะ)

โจทย์อาจารย์บังคับให้โค้ดดูเหมือนนักศึกษาปีหนึ่งเขียนเอง เพราะงั้น:

- ห้ามใช้ arrow function หรือ pattern ขั้นสูงใน JS ใช้ `function`, `var`, `if/else` ธรรมดาๆ พอ
- ตัวแปร/ฟังก์ชันบางตัวตั้งชื่อไทยปนอังกฤษ (เช่น `เช็คฟอร์มสมัครงาน`, `แสดงฟอร์ม`) — ตั้งใจแบบนี้แหละ
- คอมเมนต์เป็นภาษาไทยกันลืม ไม่ใช่คอมเมนต์ทางการแบบ JSDoc
- ห้ามใส่คอมเมนต์แบบ `// TODO: implement later` หรือ `// generated by AI` เด็ดขาด โจทย์ห้ามไว้

จะเพิ่มฟีเจอร์ใหม่ก็เขียนสไตล์เดียวกันต่อไปเรื่อยๆ นะ อย่าหักมุมไปเขียนแบบมืออาชีพกลางทาง เดี๋ยวโค้ด
มันดูแปลกๆ ปนกันไปมา
